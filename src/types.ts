export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  features: string;
  coverage: string;
  packaging: string;
  applicationTools: string;
  image: string;
}
